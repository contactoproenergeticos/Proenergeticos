name: Sync precios combustible

on:
  schedule:
    # ~07:00 hora Mazatlán (variación por horario de verano; ajustar si hace falta)
    - cron: '0 12 * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - name: Exportar leyenda Mazatlán (GITHUB_ENV)
        run: node scripts/sync-precios-combustible.mjs --write-github-env
      - name: Ejecutar sincronización
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          PREC_SYNC_FECHA_ACTUALIZACION: ${{ env.PREC_SYNC_FECHA_ACTUALIZACION }}
          PREC_SYNC_HORA_ACTUALIZACION: ${{ env.PREC_SYNC_HORA_ACTUALIZACION }}
        run: node scripts/sync-precios-combustible.mjs
